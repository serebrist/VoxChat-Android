<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <!-- Camera & Media -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- Audio & Microphone -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Contacts -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />

    <!-- Phone State -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- Notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Foreground Service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Wake Lock -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Boot Completed (for push service) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Biometric -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- Screen Capture (for screen sharing) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />

    <application
        android:name=".VoxChatApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VoxChat"
        android:usesCleartextTraffic="false"
        tools:targetApi="34">

        <!-- Main Activity -->
        <activity
            android:name=".ui.activity.MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Splash Screen Activity -->
        <activity
            android:name=".ui.activity.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.VoxChat.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth Activity -->
        <activity
            android:name=".ui.activity.AuthActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Chat Activity -->
        <activity
            android:name=".ui.activity.ChatActivity"
            android:exported="false"
            android:parentActivityName=".ui.activity.MainActivity"
            android:windowSoftInputMode="adjustResize" />

        <!-- Group Info Activity -->
        <activity
            android:name=".ui.activity.GroupInfoActivity"
            android:exported="false"
            android:parentActivityName=".ui.activity.MainActivity" />

        <!-- Profile Activity -->
        <activity
            android:name=".ui.activity.ProfileActivity"
            android:exported="false"
            android:parentActivityName=".ui.activity.MainActivity" />

        <!-- Settings Activity -->
        <activity
            android:name=".ui.activity.SettingsActivity"
            android:exported="false"
            android:parentActivityName=".ui.activity.MainActivity" />

        <!-- Media Preview Activity -->
        <activity
            android:name=".ui.activity.MediaPreviewActivity"
            android:exported="false"
            android:theme="@style/Theme.VoxChat.FullScreen" />

        <!-- Full Screen Media Activity -->
        <activity
            android:name=".ui.activity.FullScreenMediaActivity"
            android:exported="false"
            android:theme="@style/Theme.VoxChat.FullScreen" />

        <!-- Incoming Call Activity -->
        <activity
            android:name=".ui.activity.IncomingCallActivity"
            android:exported="false"
            android:theme="@style/Theme.VoxChat.FullScreen"
            android:showOnLockScreen="true"
            android:turnScreenOn="true" />

        <!-- Outgoing Call Activity -->
        <activity
            android:name=".ui.activity.OutgoingCallActivity"
            android:exported="false"
            android:theme="@style/Theme.VoxChat.FullScreen" />

        <!-- Active Call Activity -->
        <activity
            android:name=".ui.activity.ActiveCallActivity"
            android:exported="false"
            android:theme="@style/Theme.VoxChat.FullScreen"
            android:screenOrientation="sensor" />

        <!-- Calls History Activity -->
        <activity
            android:name=".ui.activity.CallsHistoryActivity"
            android:exported="false"
            android:parentActivityName=".ui.activity.MainActivity" />

        <!-- Create Group Activity -->
        <activity
            android:name=".ui.activity.CreateGroupActivity"
            android:exported="false"
            android:parentActivityName=".ui.activity.MainActivity" />

        <!-- Manage Participants Activity -->
        <activity
            android:name=".ui.activity.ManageParticipantsActivity"
            android:exported="false"
            android:parentActivityName=".ui.activity.GroupInfoActivity" />

        <!-- Story Viewer Activity -->
        <activity
            android:name=".ui.activity.StoryViewerActivity"
            android:exported="false"
            android:theme="@style/Theme.VoxChat.FullScreen" />

        <!-- Push Notification Receiver -->
        <receiver
            android:name=".util.PushNotificationReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.voxchat.messenger.PUSH_NOTIFICATION" />
            </intent-filter>
        </receiver>

        <!-- Boot Receiver -->
        <receiver
            android:name=".util.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- File Provider -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Foreground Service for Calls -->
        <service
            android:name=".util.CallForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="camera|microphone" />

    </application>

</manifest>
